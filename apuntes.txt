Introduction to python

Case sensitive
Spacing is very important


Data types and operators
	- Print () - displays inout value as text 
	- normal aruthmecs operators
	- % gives the remain Number in a división
	- // rounds the result of the division
Variables
mv_
Name them in minúsculas con guiones bajos

mv_population += (significa que estas modificando la variable ya establecida)


Data types
float= numero decimal
int= numero sin punto
En python hay dos tipos de datos, float and int. Si escribes type () te dice qué tipo es
Puedes convertir flotar a int y viceversa

Buenas practicas
print() - sin espacio

In general, there are two types of errors to look out for
Exceptions
Syntax
An Exception is a problem that occurs when the code is running, but a 'Syntax Error' is a problem detected when Python checks the code before it runs it.
Data types: bool. Has a valué of true or false
age = 14
is_teen = age > 12 and age < 20
print(is_teen) # True

Compararon operators: >
<
<=
>=
== !=

Lógica operators
And, or, not

To work in text with python you need string.
String is an inmutable ordered sequence of characters (letter, numbers, etc).
You can use ‘x”y”x’, to cuota within cuota 
If in there text there is one: “ i Think you/‘re” y así ya sabe que es texto

combine strings
repeat strings

Lenght -> len()
Tells the lenght of a word pero se almacena en una variable

lenght_udacity=len(“udacity”)
resultado=7

The len() function
len() is a built-in Python function that returns the length of an object, like a string. The length of a string is the number of characters in the string. This will always be an integer.
There is an example above, but here's another one:
print(len("ababa") / len("ab"))
2.5


Method
Función that belongs to an object 
.format ()
